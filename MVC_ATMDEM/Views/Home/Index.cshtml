@{
    ViewBag.Title = "Home Page";
}

<div class="row">
    <div class="col-md-6 margin-top-20">
        <a href="#" class="btn btn-primary btn-lg btn-block"><span class="glyphicon glyphicon-circle-arrow-down"></span>Withdrawl</a>
        <a href="#" class="btn btn-primary btn-lg btn-block"><span class="glyphicon glyphicon-flash"></span>Quick Cash</a>
        <a href="@Url.Action("Details", "CHeckingAccount")" class="btn btn-primary btn-lg btn-block"><span class="glyphicon glyphicon-question-sign"></span>Balance Inquiry</a>
    </div>
    <div class="col-md-6 margin-top-20">
        <a href="#" class="btn btn-primary btn-lg btn-block"><span class="glyphicon glyphicon-print"></span>Print Statement</a>
        <a href="#" class="btn btn-primary btn-lg btn-block"><span class="glyphicon glyphicon-transfer"></span>Transfer funds</a>
        <a href="#" class="btn btn-primary btn-lg btn-block"><span class="glyphicon glyphicon-circle-arrow-up"></span>Deposit</a>
    </div>
</div>

@*<div class="row">
    <div class="col-md-4">
        <h2>Register</h2>
        <p>
            OPEN AN ACCOUNT IN MY BANK        
        </p>
        <p>
            @Html.ActionLink("Register", "Register", "Account", null, new {@class = "btn btn-default"})
        </p>
    </div>
    <div class="col-md-4">
        <h2>Log in</h2>
        <p>Access a existing account.</p>
        @Html.ActionLink("Log In", "Login", "Account", null, new {@class = "btn btn-default"})
    </div>
</div>*@

@*You are using @Request.Browser.Browser, and that's ok

@{
    var amounts = new List<Decimal> {100, 25.50m, -40, 275.99m};
}


<ul>
    @foreach (decimal amount in amounts)
    {
        <li>@formatAmount(amount)</li>
    }
</ul>


@helper formatAmount(decimal amount)
{
    var color = "green";
    if (amount < 0)
    {
        color = "red";
    }
    <span style="color: @color">@String.Format("{0:c}", amount)</span>
}*@